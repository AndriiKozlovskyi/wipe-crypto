server:
  port: 8081

base-path: /api/v1

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
    register-with-eureka: true

spring:
  application:
    name: auth-service